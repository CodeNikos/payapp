# Copiar a .env para desarrollo local: cp .env.example .env

# App
DEBUG=True
SECRET_KEY=change-me-generate-with-secrets-token-urlsafe-64
JWT_SECRET_KEY=change-me-generate-with-secrets-token-urlsafe-64

# Database (local)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=payapp
DB_USER=payadmin
DB_PASSWORD=change-me

# Seenode (sin corchetes JSON; también acepta CSV o JSON)
# ALLOWED_HOSTS=payapp.seenode.app
# ALLOWED_ORIGINS=https://payapp.seenode.app
