# Copiar a .env para desarrollo local: cp .env.example .env

# App
DEBUG=True
SECRET_KEY=change-me-generate-with-secrets-token-urlsafe-64
JWT_SECRET_KEY=change-me-generate-with-secrets-token-urlsafe-64

# Database (local)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=payapp
DB_USER=payadmin
DB_PASSWORD=change-me

# Seenode: usar credenciales de la BD gestionada y el hostname del Web Service
# ALLOWED_HOSTS=["tu-servicio.seenode.com"]
# ALLOWED_ORIGINS=["https://tu-servicio.seenode.com"]
